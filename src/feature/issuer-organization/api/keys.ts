import { referenceKeys } from "@shared/api/useReferenceData";
import type { ReferenceResource } from "@shared/types/api";

export const issuerOrganizationResource: ReferenceResource = "credential-issuer-organizations";

export const issuerOrganizationKeys = referenceKeys;
