import { referenceKeys } from "@shared/api/useReferenceData";
import type { ReferenceResource } from "@shared/types/api";

export const credentialTypeResource: ReferenceResource = "credential-types";

export const credentialTypeKeys = referenceKeys;
